import React from 'react';

const Header = (title, content) => {
    return (
        <div className="header">
             <hi>(titel)</h1>
             <p>(content)</p>
        </div>
    )
}

export default Header;